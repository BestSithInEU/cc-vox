[project]
name = "cc-vox"
version = "0.1.1"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["plugins/voice/hooks"]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
